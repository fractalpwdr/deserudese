// Assuming 'frames' is an existing array
let frames = [];

// Assuming 'frame' is a variable representing the frame to be added
let frame = { /* frame data */ };

// Pushing the 'frame' into the 'frames' array
frames.push(frame);
