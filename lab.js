// Sample JSON object
let newObject = { name: "Iceland", coords: "30,60,50,70,60,50,40,40" };

// Initializing an empty array to hold JSON objects
let jsonArray = [];

// Pushing the new object into the array
jsonArray.push(newObject);

console.log(jsonArray);
// Output: [ { name: 'Iceland', coords: '30,60,50,70,60,50,40,40' } ]
